# Issue #2 — Proceso y extracción de contenido
# Extracción por formato
pypdf>=4.2.0            # PDF
python-docx>=1.1.0     # Word (.docx)
openpyxl>=3.1.0        # Excel (.xlsx)
python-pptx>=0.6.23    # PowerPoint (.pptx)
beautifulsoup4>=4.12.0 # HTML
lxml>=5.2.0            # parser para BeautifulSoup
# Fuentes remotas
requests>=2.31.0       # fetch de documentos online (URL)

# Issue #3 — Indexación (set de DEPLOY: embeddings por API, liviano, sin torch)
chromadb>=0.5.0        # base vectorial con índice HNSW + filtro por metadatos
python-dotenv>=1.0.0   # carga de API keys desde .env
cohere>=5.0.0          # API de embeddings multilingüe (embed-multilingual-v3.0)

# Issue #5 — Generación
# (sin dependencia nueva: se reutiliza el SDK `cohere` para chat con Command-R)

# Issue #6 — Interfaz web + mantenimiento
fastapi>=0.110.0       # API HTTP del agente
python-multipart>=0.0.9 # Para subida de archivos
uvicorn>=0.30.0        # servidor ASGI liviano (apto para VM chica de OCI)

# Embeddings LOCALES (alternativa offline sin API) -> ver requirements-local.txt
# No se instala acá para mantener la VM liviana y evitar el conflicto con torch.
